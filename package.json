{
  "private": true,
  "name": "packages",
  "engines": {
    "pnpm": ">=7"
  },
  "scripts": {
    "build": "turbo run build",
    "changeset": "changeset",
    "changeset:publish": "changeset publish",
    "changeset:version": "changeset version"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "@types/node": "^18.11.0",
    "rimraf": "^3.0.2",
    "ts-jest": "29.0.3",
    "turbo": "^1.5.6",
    "typescript": "^4.8.4"
  }
}
