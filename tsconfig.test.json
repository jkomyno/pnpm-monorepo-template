{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "lib": ["ESNext"],
    "module": "ESNext",
    "target": "ESNext",
    "moduleResolution": "Bundler",

    // Include Node.js and test-specific types.
    "types": ["node", "vitest/globals"]
  }
}
